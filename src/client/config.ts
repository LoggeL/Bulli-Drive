export const CONFIG = {
    carSpeed: 0.5,
    carTurnSpeed: 0.05,
    cameraHeight: 25,
    cameraDistance: 35,
    cameraLookAtY: 0,
    shadowMapSize: 2048,
    // Dynamic WS URL: Use current hostname/port
    serverUrl: window.location.origin.replace(/^http/, 'ws')
};
